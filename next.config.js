/** @type {import('next').NextConfig} */
const nextConfig = {
  // Server Actions are enabled by default in Next.js 14+
  // Removed experimental.serverActions (deprecated)
}

module.exports = nextConfig
